const path = require("path");

module.exports = {
  config: path.resolve("./src/", "config.json"),
  "migrations-path": path.resolve("./src", "migrations"), //migrations tạo các bảng trong database
  "models-path": path.resolve("./src", "models"),
  "seeders-path": path.resolve("./src", "seeders"), //seederstạo dữ liệu fake cho we
};
